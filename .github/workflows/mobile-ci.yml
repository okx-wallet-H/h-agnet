name: Mobile CI

on:
  pull_request:
  push:
    branches:
      - main
      - codex-h-agent-framework

permissions:
  contents: read

jobs:
  checks:
    name: Typecheck And Lint
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint
